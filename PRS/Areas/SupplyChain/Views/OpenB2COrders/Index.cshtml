@model IP.Areas.SupplyChain.Models.OpenB2COrders

@{
    ViewBag.Title = "Index";
    Layout = null;
}
<style>
    tr {
        white-space: nowrap;
    }
</style>
<div id="parentDiv" class="container-fluid">
    <div id="dataList" class="box panel panel-default">
        <div class="box-header with-border bg-blue-gradient">
            <button type="button" class="btn btn-box-tool" title="Option Page" onclick="$('#option').modal('show');">
                <span class="fa fa-filter fa-lg white"></span>
            </button>
            <h6 class="box-title"><strong>@ViewBag.ReportTitle  </strong><strong id="filterString"></strong></h6>
            <div id="buttons" class="box-tools pull-right">

            </div>
        </div>
        <div class="box-body" id="lstP">
            <table id="lstOpenB2COrders" class="table table-bordered table-striped text-sm table-hover" >
                <thead class="sorted-asc">
                    <tr>
                        <th></th>
                        <th class="notexport"></th>
                        <th class="notexport"></th>
                        <th></th>
                        <th></th>

                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>

                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>

                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>

                        <th></th>

                    </tr>
                </thead>
                <tbody id="tblBody"></tbody>
            </table>
        </div>
    </div>
</div>
<script>

    ToggleDataList(false);

    //translateText("Hello, world!")
    //    .then(translatedText => //console.log(translatedText))
    //    .catch(error => //console.log(error));
    LoadData();
    function LoadData() {
        $('#tblBody').empty();
        $('#option').modal('hide');

        var _programId = $('#ddProgram').val();
        var _programName = $("#ddProgram option:selected").text();

        $.ajax({
            cache: false,
            type: 'GET',
            url: '/OpenB2COrders/GetList',
            data: {
                programId: _programId,
                programName: _programName
            },
            dataType: 'json',
            success: function (data) {

                // Assuming you want to append the filterString to some element as before
                $('#filterString').append(data.filterString);
                //console.log(data.lstOpenB2COrders);

                if (_programId == '10042') {
                   
                    var columnDef = [
            { "data": "program", "title": "Program", "width": "100px" },
            { "data": "ProgramID", "title": "Program Id", "width": "80px", "visible": false },
            { "data": "roheaderId", "title": "roheaderId", "width": "80px", "visible": false },

            {
                "data": "CustomerReference", "title": "Customer Ref.", "width": "150px",
                render: function (data, type, row) {
                    var url = '<a href= "\\SupplyChain\\WO\\Detail?Id=' + row["roheaderId"] + '&custRef=' + row["CustomerReference"] + '&ProgramId=' + row["ProgramID"] + '&page=list" target=_blank > ' + data + '</a>';
                    return url;
                }
            },
            { "data": "CreateDate", "title": "RO Created On", "width": "120px", "className": "text-right" },
            { "data": "AddressID", "title": "RO Address Id", "width": "80px" },
            { "data": "ID", "title": "ID", "width": "60px" },
            { "data": "AddressID", "title": "Address Id", "width": "80px" },
            { "data": "AddressType", "title": "Address Type", "width": "100px" },
            {
                "data": "Name", "title": "Name", 

                // Working Approach:
                render: function (data, type, row, meta) {

                    //console.log(`Rendering cell for data: "${data}"`);

                    // Create a unique ID for the placeholder
                    const uniqueId = `translate_${meta.row}_${meta.col}`;
                    ////console.log(uniqueId);
                    const placeholder = `<span id="${uniqueId}">${data}</span>`;

                    // Only translate if data is not empty
                    if (data && data.trim() !== "") {
                        // Translate the "Bill To Site Name"
                        translateText(data, 'ja', 'en').then(translatedText => {
                            //console.log(`Updating placeholder with translated text: "${translatedText}"`);
                            // Update the placeholder with the translated text
                            document.getElementById(uniqueId).innerText = translatedText;
                        }).catch(error => {
                            //console.error('Translation error:', error);
                        });
                    } else {
                        //console.log('No data to translate');
                    }

                    // Return the placeholder HTML
                    return placeholder;
                }
            },
            {
                "data": "Address1", "title": "Address 1", 

                // Working Approach:
                render: function (data, type, row, meta) {

                    //console.log(`Rendering cell for data: "${data}"`);

                    // Create a unique ID for the placeholder
                    const uniqueId = `translate_${meta.row}_${meta.col}`;
                    ////console.log(uniqueId);
                    const placeholder = `<span id="${uniqueId}">${data}</span>`;

                    // Only translate if data is not empty
                    if (data && data.trim() !== "") {
                        // Translate the "Bill To Site Name"
                        translateText(data, 'ja', 'en').then(translatedText => {
                            //console.log(`Updating placeholder with translated text: "${translatedText}"`);
                            // Update the placeholder with the translated text
                            document.getElementById(uniqueId).innerText = translatedText;
                        }).catch(error => {
                            //console.error('Translation error:', error);
                        });
                    } else {
                        //console.log('No data to translate');
                    }

                    // Return the placeholder HTML
                    return placeholder;
                }
            },
            {
                "data": "Address2", "title": "Address 2", 

                // Working Approach:
                render: function (data, type, row, meta) {

                    //console.log(`Rendering cell for data: "${data}"`);

                    // Create a unique ID for the placeholder
                    const uniqueId = `translate_${meta.row}_${meta.col}`;
                    ////console.log(uniqueId);
                    const placeholder = `<span id="${uniqueId}">${data}</span>`;

                    // Only translate if data is not empty
                    if (data && data.trim() !== "") {
                        // Translate the "Bill To Site Name"
                        translateText(data, 'ja', 'en').then(translatedText => {
                            //console.log(`Updating placeholder with translated text: "${translatedText}"`);
                            // Update the placeholder with the translated text
                            document.getElementById(uniqueId).innerText = translatedText;
                        }).catch(error => {
                            //console.error('Translation error:', error);
                        });
                    } else {
                        //console.log('No data to translate');
                    }

                    // Return the placeholder HTML
                    return placeholder;
                }
            },
            {
                "data": "City", "title": "City",

                // Working Approach:
                render: function (data, type, row, meta) {

                    //console.log(`Rendering cell for data: "${data}"`);

                    // Create a unique ID for the placeholder
                    const uniqueId = `translate_${meta.row}_${meta.col}`;
                    ////console.log(uniqueId);
                    const placeholder = `<span id="${uniqueId}">${data}</span>`;

                    // Only translate if data is not empty
                    if (data && data.trim() !== "") {
                        // Translate the "Bill To Site Name"
                        translateText(data, 'ja', 'en').then(translatedText => {
                            //console.log(`Updating placeholder with translated text: "${translatedText}"`);
                            // Update the placeholder with the translated text
                            document.getElementById(uniqueId).innerText = translatedText;
                        }).catch(error => {
                            //console.error('Translation error:', error);
                        });
                    } else {
                        //console.log('No data to translate');
                    }

                    // Return the placeholder HTML
                    return placeholder;
                }

            },
            {
                "data": "State", "title": "State",

                // Working Approach:
                render: function (data, type, row, meta) {

                    //console.log(`Rendering cell for data: "${data}"`);

                    // Create a unique ID for the placeholder
                    const uniqueId = `translate_${meta.row}_${meta.col}`;
                    ////console.log(uniqueId);
                    const placeholder = `<span id="${uniqueId}">${data}</span>`;

                    // Only translate if data is not empty
                    if (data && data.trim() !== "") {
                        // Translate the "Bill To Site Name"
                        translateText(data, 'ja', 'en').then(translatedText => {
                            //console.log(`Updating placeholder with translated text: "${translatedText}"`);
                            // Update the placeholder with the translated text
                            document.getElementById(uniqueId).innerText = translatedText;
                        }).catch(error => {
                            //console.error('Translation error:', error);
                        });
                    } else {
                        //console.log('No data to translate');
                    }

                    // Return the placeholder HTML
                    return placeholder;
                }
            },
            { "data": "Country", "title": "Country", "width": "80px" },
            { "data": "Zip", "title": "Zip", "width": "80px" },
            { "data": "Email", "title": "Email", "width": "150px" },
            { "data": "Phone", "title": "Phone", "width": "100px" },
            { "data": "Username", "title": "Username", "width": "100px" },
            { "data": "CreateDate", "title": "Create On", "width": "120px", "className": "text-right" },
            { "data": "LastActivityDate", "title": "Last Activity On", "width": "150px", "className": "text-right" }
        ];
        }
        else {
            var columnDef = [
                { "data": "program", "title": "Program", "width": "100px" },
                { "data": "ProgramID", "title": "Program Id", "width": "80px", "visible": false },
                { "data": "roheaderId", "title": "roheaderId", "width": "80px", "visible": false },

                {
                    "data": "CustomerReference", "title": "Customer Ref.", "width": "150px",
                    render: function (data, type, row) {
                        var url = '<a href= "\\SupplyChain\\WO\\Detail?Id=' + row["roheaderId"] + '&custRef=' + row["CustomerReference"] + '&ProgramId=' + row["ProgramID"] + '&page=list" target=_blank > ' + data + '</a>';
                        return url;
                    }
                },
                { "data": "CreateDate", "title": "RO Created On", "width": "120px", "className": "text-right" },
                { "data": "AddressID", "title": "RO Address Id", "width": "80px" },
                { "data": "ID", "title": "ID", "width": "60px" },
                { "data": "AddressID", "title": "Address Id", "width": "80px" },
                { "data": "AddressType", "title": "Address Type", "width": "100px" },
                {
                    "data": "Name", "title": "Name", "width": "100px"
                },
                {
                    "data": "Address1", "title": "Address 1", "width": "150px"
                },
                {
                    "data": "Address2", "title": "Address 2", "width": "150px"
                },
                {
                    "data": "City", "title": "City", "width": "100px"

                },
                {
                    "data": "State", "title": "State", "width": "80px"
                },
                { "data": "Country", "title": "Country", "width": "80px" },
                { "data": "Zip", "title": "Zip", "width": "80px" },
                { "data": "Email", "title": "Email", "width": "150px" },
                { "data": "Phone", "title": "Phone", "width": "100px" },
                { "data": "Username", "title": "Username", "width": "100px" },
                { "data": "CreateDate", "title": "Create On", "width": "120px", "className": "text-right" },
                { "data": "LastActivityDate", "title": "Last Activity On", "width": "150px", "className": "text-right" }
            ];
        }


                ToggleDataList(true);

                MakeDataGrid('lstOpenB2COrders', data.lstOpenB2COrders, 0, columnDef, true, true, false);

                if (data["ErrorMessage"] != null) {
                    $('.dataTables_empty > p').text(data["ErrorMessage"]);
                }
            },
            error: function (r) {
                console.error('Error:', r);
            },
            failure: function (r) {
                console.error('Failure:', r);
            }
        });
    }
    //working approach
    function translateText(text, targetLang = 'ja', sourceLang = 'auto') {
        //const url = `https://translate.googleapis.com/translate_a/single?client=gtx&sl=auto&tl=${targetLang}&dt=t&q=${encodeURI(text)}`;
        const url = `https://translate.googleapis.com/translate_a/single?client=gtx&sl=${sourceLang}&tl=${targetLang}&dt=t&q=${encodeURI(text)}`;
        ////console.log(`Translating text: "${text}" to language: "${targetLang}" using URL: ${url}`);

        return fetch(url)
            .then(response => {
                ////console.log('Received response:', response);
                return response.json();
            })
            .then(data => {
                ////console.log('Received data:', data);
                const translatedText = data[0][0][0];
                //console.log(`Translated text: "${translatedText}"`);
                return translatedText;
            })
            .catch(error => {
                //console.error('Error:', error);
            });
    }

</script>



