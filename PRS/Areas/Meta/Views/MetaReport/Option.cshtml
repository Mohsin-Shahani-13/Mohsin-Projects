@model IP.Areas.Meta.Models.MetaReport
@{
    ViewBag.Title = "Option";
    Layout = null;
}
<style>
    .pad {
        padding-bottom: 15px;
        padding: 2px;
        padding-top: 10px;
    }
</style>
<table class="fullWidth">
    <tr>
        <td colspan="4" class="pad">
            @Html.LabelFor(model => model.contract, htmlAttributes: new { @class = "col-4 col-form-lable" })
            @Html.DropDownList("ddContract", null, new { id = "ddContract", @name = "ddContract", @class = "form-control", @onkeydown = "GoOnEnter()" })
        </td>
    </tr>
</table>
<br />
<table width="100%">
    <tr>
        <td align="center">
            <div id="lblMsg" class="optionMsg">
            </div>
        </td>
        <td width="70" align="right">
            <a id="btnGo" role="button" class="btn btn-primary">Confirm</a>
        </td>
        <td width="100" align="center">
            <a id="btnCancel" data-dismiss="modal" class="cancel">Cancel</a>
        </td>
    </tr>
</table>
<input id="rptcode" type="hidden" value='@Request.QueryString["rptCode"]' />
<script>

    function GoOnEnter() {
        if (event.keyCode == 13) {
            Go();
        }
    }

    $('#btnGo').click(function () {
        Go();
    });

    function Go() {

        var contract = $("#ddContract option:selected").val();
        var programName = $("#ddContract option:selected").text();

        $('#option').modal('hide');
        OpenPage('\\Meta\\MetaReport\\Index?contract=' + contract + '&programName=' + programName + '&menuTitle=' + $('.modal-header').html() + '&rptCode=' + '@Request.QueryString["rptCode"]')
    }
</script>

