@model IP.Areas.Meta.Models.SOsStatus
@{
    ViewBag.Title = "Option";
    Layout = null;
}


<style>
    .pad {
        width: 260px;
    }
</style>
<table class="fullWidth">
    <tr>
        <td class="pad">
            <strong>From:</strong>
            <div class='input-group date' id='dtpFrom'>
                @Html.TextBoxFor(model => model.fromDt, htmlAttributes: new { @class = "form-control" })
                <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
            </div>
        </td>
        <td class="pad">
            <strong>To:</strong>
            <div class='input-group date' id='dtpTo'>
                @Html.TextBoxFor(model => model.toDt, htmlAttributes: new { @class = "form-control" })
                <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
            </div>
        </td>

        <td class="dateFont ">
            <div>
                <br />
                <br />
                <label for="_DockAllDate" style="margin-left:0px">
                    <input type="checkbox" id="DockAllDate" name="DockAllDate" onclick="EnableDisableOrdCreat_2(this)" />
                    All
                </label>
            </div>
        </td>
    </tr>
    <tr>
        <td class="pad">
            <strong>Event Date From:</strong>
            <div class='input-group date disabled' id='dtpFrom'>
                @Html.TextBoxFor(model => model.EvDateFrom, htmlAttributes: new { @class = "form-control" })
                <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
            </div>
        </td>
        <td class="pad">
            <strong>Event Date To:</strong>
            <div class='input-group date disabled' id='dtpTo'>
                @Html.TextBoxFor(model => model.EvDateTo, htmlAttributes: new { @class = "form-control" })
                <span class="input-group-addon">
                    <span class="glyphicon glyphicon-calendar"></span>
                </span>
            </div>
        </td>
    </tr>
    <tr>
        <td colspan="4" class="pad">
            @Html.LabelFor(model => model.custRef, new { @class = "col-4 col-form-label" })
            <input type="text" class="form-control" id="custRef" name="custRef" autofocus onkeydown="GoOnEnter()" />

        </td>
    </tr>
</table>
<table width="100%">
    <tr>
        <td align="center">
            <div id="lblMsg" class="optionMsg">
            </div>
        </td>
        <td align="center">
            <span id="myValidationMessage" class="optionMsg"></span>
        </td>
        <td width="70" align="right">
            <a id="btnGo" role="button" class="btn btn-primary">Confirm</a>
        </td>
        <td width="100" align="center">
            <a id="btnCancel" data-dismiss="modal" class="cancel">Cancel</a>
        </td>
    </tr>
</table>
<script>
    $(document).ready(function () {
        document.getElementById("EvDateFrom").disabled = true;
        document.getElementById("EvDateTo").disabled = true;

        $('#DockAllDate').on('change', function () {
            var isChecked = $('#DockAllDate').is(':checked');
            if (isChecked == true) {
                document.getElementById("EvDateFrom").disabled = false;
                document.getElementById("EvDateTo").disabled = false;
            }
            else {
                document.getElementById("EvDateFrom").disabled = true;
                document.getElementById("EvDateTo").disabled = true;
            }
        })
    })
    function EnableDisableOrdCreat_2(dockIsAllDate) {
        var dockIsAllDate = $('#DockAllDate').is(':checked');
        document.getElementById("fromDt").disabled = dockIsAllDate;
        var nodes1 = document.getElementById("fromDt").getElementsByTagName('*');
        document.getElementById("toDt").disabled = dockIsAllDate;
        var nodes1 = document.getElementById("toDt").getElementsByTagName('*');
        for (var i = 0; i < nodes1.length; i++) {
            nodes1[i].disabled = dockIsAllDate;
        }
    };


    //function textBoxValidation() {
    //    $('#myValidationMessage').empty();
    //    if ($('#DockAllDate').is(':checked') && $('#custRef').val() === '') {

    //        $('#myValidationMessage').append('Please input the Value');
    //        return false;
    //    } else {

    //        $('#myValidationMessage').empty();
    //        return true;
    //    }
    //}



    $('#option').on('shown.bs.modal', function () {

    });

    function GoOnEnter() {
        if (event.keyCode == 13) {
            var retVal = OptionsIsValid();
            //var retValue = textBoxValidation();
            if (retVal == true) {
                Go();
            }


        }
    }
    $('#btnGo').click(function () {
        var retVal = OptionsIsValid();
        //var retValue = textBoxValidation();
        if (retVal == true) {
            Go();
        }
    });

    $("#dtpTo, #dtpFrom").mouseover(function () {
        $(this).css('cursor', 'pointer');
    });

    $("#dtpTo, #dtpFrom").mouseover(function () {
        $(this).css('cursor', 'pointer');
    });



    //For Date Picker
    dateFrom();
    dateTo();
    function dateFrom() {
        var date = new Date();
        var newdate = new Date(date);
        //For From Date
        newdate.setDate(newdate.getDate() - 1);

        var dd = (newdate.getFullYear(newdate.getMonth()), '0' + 1);
        var mm = ('0' + (newdate.getMonth() + 1)).slice(-2);
        var y = newdate.getFullYear();
        var strat = newdate

        var someFormattedDate = y + '.' + mm + '.' + dd;

        document.getElementById('fromDt').value = someFormattedDate;
    }
    function dateTo() {
        var date = new Date();
        var newdate = new Date(date);
        //For To Date
        newdate.setDate(newdate.getDate()-1);

        var dd = ('0' + newdate.getDate()).slice(-2);
        var mm = ('0' + (newdate.getMonth() + 1)).slice(-2);
        var y = newdate.getFullYear();
        var strat = newdate

        var someFormattedDate = y + '.' + mm + '.' + dd;

        document.getElementById('toDt').value = someFormattedDate;
    }

    $("#dtpTo, #dtpFrom").datepicker({
        format: 'yyyy.mm.dd',
        autoclose: true,
        startDate: '-120d',
        endDate: '-1d'
    });

    function isDate(txtDate) {
        if (txtDate.length != 10) {
            return false;
        }
        if (txtDate.substring(4, 5) !== '.' || txtDate.substring(7, 8) !== '.') {
            return false;
        }
        return true;
    }

    function OptionsIsValid() {

        $('#lblMsg').empty();
        var fDate = $('#fromDt').val();
        var tDate = $('#toDt').val();

        if (fDate.length <= 0 || tDate.length <= 0 || !isDate(fDate) || !isDate(tDate) || fDate > tDate) {
            $('#lblMsg').append('Enter a valid date');
            return false;
        }
        else {
            $('#lblMsg').empty();
            return true;
        }
    }

    function Go() {
        var programId = $('#ProgramID').val();
        var ProgramName = $("#ProgramID option:selected").text();
        var fDate = $('#fromDt').val();
        var tDate = $('#toDt').val();
        var ischecked = $('#DockAllDate').is(':checked');
        var custRef = $('#custRef').val();
        var EvDateFrom = $('#EvDateFrom').val();
        var EvDateTo = $('#EvDateTo').val();

        $('#option').modal('hide');
        OpenPage('\\Meta\\SOsStatus\\Index?programId=' + programId + '&programName=' + ProgramName + '&frmDt=' + fDate + ' &toDate=' + tDate + '&ischecked=' + ischecked + '&custRef=' + custRef + '&EvDateFrom=' + EvDateFrom + ' &EvDateTo=' + EvDateTo + '&menuTitle=' + $('.modal-header').html() + '&rptCode=' + '@Request.QueryString["rptCode"]')
    }
</script>
